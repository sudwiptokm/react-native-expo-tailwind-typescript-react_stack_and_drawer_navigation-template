import React from "react";
import { View } from "react-native";

type Props = {};

const Hr = (props: Props) => {
  return <View className="border-b border-gray-500 flex-1" />;
};

export default Hr;
