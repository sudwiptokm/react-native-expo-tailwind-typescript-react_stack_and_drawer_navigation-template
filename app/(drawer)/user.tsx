import { Text, View } from "react-native";

import React from "react";

type Props = {};

const user = (props: Props) => {
  return (
    <View>
      <Text>user</Text>
    </View>
  );
};

export default user;
